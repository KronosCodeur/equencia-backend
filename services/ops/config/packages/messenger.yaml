framework:
    messenger:
        failure_transport: failed

        transports:
            async:
                dsn: '%env(REDIS_URL)%'
                options:
                    stream: '%env(OPS_REDIS_STREAM)%'
                    group: ops_workers
                    consumer: ops_%env(HOSTNAME)%
                retry_strategy:
                    max_retries: 3
                    delay: 1000
                    multiplier: 2
                    max_delay: 0

            failed:
                dsn: '%env(REDIS_URL)%'
                options:
                    stream: ops_failed

        routing:
            App\Application\Command\*: async
            App\Infrastructure\Messenger\Message\*: async
